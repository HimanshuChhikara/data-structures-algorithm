// let map = new Map([
//     ['Himanshu',1],
//     ['Devansh',2],
//     ['Ravi',3],
//     ['Dipesh',4]
// ]);

// for(let count of map.keys()){
//     console.log(count);
// }

function map(str){
    var len = str.length;
    var count = new Map();

    for(let num of str){
        var result = count.set(num,2);
        if(count.set)
    }
}